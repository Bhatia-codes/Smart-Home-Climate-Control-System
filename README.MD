# Smart Home Climate Control System

This project simulates a smart home climate control system using fuzzy logic. It features:

- 🔥 A **FastAPI** backend with `scikit-fuzzy` for fuzzy logic-based decision-making.
- 💻 A **Next.js + React** frontend for the user interface.
- 🌐 CORS enabled to allow communication between frontend and backend.